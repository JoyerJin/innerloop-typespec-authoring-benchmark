

What should service team do when implementing polling? use AAO or location? Or support both?

What is the recommended way of writing TypeSpec in below scenarios?
1. By default, should we generate update method which uses patch template? There is createOrUpdate(e.g. [ArmResourceCreateOrUpdateAsync](https://github.com/Azure/typespec-azure/blob/main/packages/typespec-azure-resource-manager/lib/operations.tsp#L155)) related templates and patch templates? If service team wants to add create and update operation for an ARM resource. What is the recommended TypeSpec? Having both operations - one uses createOrUpdate teamplate and the other uses patch teamplate, or just having one operation which uses createOrUpdate template?
1. The difference between `createOrUpdate` and `createOrReplace`. e.g. for async dataplane, [LongRunningResourceCreateOrUpdate](https://github.com/Azure/typespec-azure/blob/main/packages/typespec-azure-core/lib/operations.tsp#L131) and [LongRunningResourceCreateOrReplace](https://github.com/Azure/typespec-azure/blob/main/packages/typespec-azure-core/lib/operations.tsp#L131). 
1. Which one is recommended? `createOrUpdate` or `createOrReplace`? In typespec-azure website, `createOrUpdate` is recommended. But in [playground](https://azure.github.io/typespec-azure/playground/?options=%7B%22linterRuleSet%22%3A%7B%22extends%22%3A%5B%22%40azure-tools%2Ftypespec-azure-rulesets%2Fresource-manager%22%5D%7D%7D&sample=Azure+Resource+Manager+framework&vs=%7B%7D), `createOrReplace` is used. I also checked ARM templates code: [ArmResourceCreateOrReplaceAsync](https://github.com/Azure/typespec-azure/blob/main/packages/typespec-azure-resource-manager/lib/operations.tsp#L236) and [ArmResourceCreateOrUpdateAsync](https://github.com/Azure/typespec-azure/blob/main/packages/typespec-azure-resource-manager/lib/operations.tsp#L155), I tried to compare the two templates' code, seems there is no difference.
1. There are multiple patch related templates. For example, [ArmResourcePatchAsync](https://github.com/Azure/typespec-azure/blob/main/packages/typespec-azure-resource-manager/lib/operations.tsp#L295) and [ArmCustomPatchAsync](https://github.com/Azure/typespec-azure/blob/main/packages/typespec-azure-resource-manager/lib/operations.tsp#L323). 
1. Async or sync by default for create, delete, action, update?

